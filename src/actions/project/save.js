import { createAction } from 'redux-act';

export const Start = createAction('Project Save - started');
export const Error = createAction('Project Save - error');
export const Complete = createAction('Project Save - complete');
